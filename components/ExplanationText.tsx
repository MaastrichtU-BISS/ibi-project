import React from 'react'
import {
  Text,
} from 'native-base'

export const ExplanationText = (props) => (
  <Text
    fontSize="8.3px"
    fontFamily="sans-serif"
    fontWeight={400}
    color="rgb(0,0,0)"
    w="full"
    {...props}
  />
)
